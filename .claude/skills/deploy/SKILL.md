# Deploy Skill
1. Run the build command and verify it succeeds
2. Check that the build output directory matches GitHub Pages config
3. Run a quick syntax/load check on index.html
4. Git add, commit with descriptive message, and push
5. Confirm push succeeded (no auth errors)
6. Report deployment status
